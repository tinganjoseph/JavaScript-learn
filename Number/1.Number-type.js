/**
 * ?To create a Number object, you use the Number constructor and use the Number constructor and pass in a number value as
 */

var numberObject = new Number(100);
console.log(numberObject);//[Number:100]
console.log(numberObject.valueOf());//100